{
  "name": "lru-cache",
  "version": "1.0.0",
  "description": "O(1) LRU cache: hashmap + doubly linked list",
  "main": "dist/LRUCache.js",
  "types": "dist/LRUCache.d.ts",
  "scripts": {
    "build": "tsc",
    "demo": "tsc && node dist/demo.js"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "@types/node": "^22.7.4"
  }
}
